#pragma once

const int WINDOW_WIDTH = 800;	//width of the window

const int WINDOW_HEIGHT = 800;	//height of the window

const int box_size = 88;		//box size (width and height)

#define SDL_MOUSE_LEFT 1	//left click
